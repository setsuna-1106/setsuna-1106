# 个人网站

直接打开 `index.html` 即可预览；将根目录文件与 `assets/` 一起部署到 GitHub Pages。

- `data.js`：项目介绍与公开仓库快照，依据 2026-09-05 的 GitHub API 和各项目 README 整理。
- `script.js`：分类筛选、主题、图片预览、仓库更新和阻尼振子解析解。
- `styles.css`：桌面与移动端布局，支持浅色、深色和减少动画偏好。
- `advance/`：原有独立实验档案，保留其入口。

“最近的仓库更新”按 GitHub `pushed_at` 排序。刷新按钮读取公开 API 并缓存成功结果；网络不可用时保留带日期的快照。项目介绍由 `data.js` 手动维护。

实验使用欠阻尼解析解，初始条件 `x(0)=1, v(0)=0, m=1`。能量由同一时刻的位移、速度计算；参数变化会重置时间，首屏和实验动画独立暂停。

图标为本地 Lucide 0.468.0，许可证位于 `assets/vendor/lucide.LICENSE`。
